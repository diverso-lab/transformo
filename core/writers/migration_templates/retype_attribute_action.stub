
    <transformation type="{{ action.transformation_type()}}" id="{{ action.__class__.__name__ }}">
        <action type="{{ action.action_type()}}">
            <entity>{{ action.entity_id() }}</entity>
            <attribute>{{ action.attribute_name() }}</attribute>
            <retype>{{ action.retype_name() }}</retype>
        </action>
    </transformation>
